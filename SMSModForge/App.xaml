<Application x:Class="SMSModForge.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!--
            Theme brushes. Defaults match the "Light" theme; ThemeManager
            overwrites them at startup and on every theme switch so the
            DynamicResource bindings below re-skin the window live. See
            Services/ThemeManager.cs for the catalogue.

            Dark-theme correctness note: there is ONE text foreground
            (Theme.Text) applied to every label/input. WPF leaks an implicit
            TextBlock foreground into the string content of buttons, tab
            headers and menu items, so the only reliable way to avoid invisible
            text is to make every text-bearing surface themed — Button and
            TabItem carry simple templates and Menu/MenuItem/inputs get a themed
            Background. Then the single light foreground always lands on a dark
            surface (and a light theme's dark foreground always on a light one).
        -->
        <SolidColorBrush x:Key="Theme.Window"     Color="#F2F2F2"/>
        <SolidColorBrush x:Key="Theme.Chrome"     Color="#E4E4E4"/>
        <SolidColorBrush x:Key="Theme.ChromeText" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="Theme.Surface"    Color="#FBFBFB"/>
        <SolidColorBrush x:Key="Theme.Accent"     Color="#B8B8B8"/>
        <SolidColorBrush x:Key="Theme.AccentText" Color="#333333"/>
        <SolidColorBrush x:Key="Theme.Text"       Color="#1E1E1E"/>
        <SolidColorBrush x:Key="Theme.Control"    Color="#FFFFFF"/>

        <!-- ══ ComboBox drop toggle. Aero ignores ComboBox.Background, so the
             box stayed white → light text on white on dark themes. This
             template honours the theme. ═══════════════════════════════════ -->
        <Style x:Key="Theme.ComboToggleButton" TargetType="ToggleButton">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="bd" Background="{DynamicResource Theme.Control}"
                                BorderBrush="{DynamicResource Theme.Accent}" BorderThickness="1" CornerRadius="2">
                            <Path HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,8,0"
                                  Data="M0,0 L4,4 L8,0 Z" Fill="{DynamicResource Theme.Text}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource Theme.AccentText}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ══ Menu item templates (themed dropdowns). Aero menus don't honour
             Background either, so dropdowns need real templates to be dark. ═ -->
        <ControlTemplate x:Key="Theme.MenuTopHeader" TargetType="MenuItem">
            <Grid>
                <Border x:Name="bd" Background="{TemplateBinding Background}" Padding="8,3">
                    <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"/>
                </Border>
                <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom"
                       IsOpen="{TemplateBinding IsSubmenuOpen}" Focusable="False" PopupAnimation="Fade">
                    <Border Background="{DynamicResource Theme.Chrome}" BorderBrush="{DynamicResource Theme.Accent}"
                            BorderThickness="1" Padding="2">
                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </Popup>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource Theme.Accent}"/>
                </Trigger>
                <Trigger Property="IsSubmenuOpen" Value="True">
                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource Theme.Accent}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <ControlTemplate x:Key="Theme.MenuSubItem" TargetType="MenuItem">
            <Border x:Name="bd" Background="{TemplateBinding Background}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="22"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border x:Name="check" Grid.Column="0" Width="16" Height="16" Margin="3" Visibility="Collapsed"
                            Background="{DynamicResource Theme.Accent}" CornerRadius="2">
                        <Path Data="M0,3 L2.5,6 L6,0" Stroke="{DynamicResource Theme.Text}" StrokeThickness="1.6"
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True"
                                      Margin="2,4" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Text="{TemplateBinding InputGestureText}" Opacity="0.6"
                               Margin="16,4,8,4" VerticalAlignment="Center"/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource Theme.Accent}"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <ControlTemplate x:Key="Theme.MenuSubHeader" TargetType="MenuItem">
            <Border x:Name="bd" Background="{TemplateBinding Background}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="22"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                    </Grid.ColumnDefinitions>
                    <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True"
                                      Margin="2,4" VerticalAlignment="Center"/>
                    <Path Grid.Column="2" Data="M0,0 L4,4 L0,8 Z" Fill="{DynamicResource Theme.Text}"
                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Right" HorizontalOffset="-3"
                           IsOpen="{TemplateBinding IsSubmenuOpen}" Focusable="False" PopupAnimation="Fade">
                        <Border Background="{DynamicResource Theme.Chrome}" BorderBrush="{DynamicResource Theme.Accent}"
                                BorderThickness="1" Padding="2">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </Popup>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource Theme.Accent}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="{DynamicResource Theme.Accent}"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="6,3"/>
        </Style>

        <!-- ── Text ─────────────────────────────────────────────────────
             Plain labels follow the theme foreground. Elements that set an
             explicit Foreground (Gray help text, coloured swatches, White on
             dark previews) keep it — a local value beats the implicit style. -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
        </Style>

        <!-- ── Windows (dialogs pick up the app background) ─────────────── -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource Theme.Window}"/>
        </Style>

        <!-- ── Input controls ───────────────────────────────────────────
             Themed Background + Foreground. The TextBox style also skins the
             editable ComboBox's inner text box (implicit styles reach template
             parts), so combo edit text stays readable on dark. -->
        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="{DynamicResource Theme.Control}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Accent}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource Theme.Text}"/>
        </Style>

        <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="IsSynchronizedWithCurrentItem" Value="False"/>
            <Setter Property="Background" Value="{DynamicResource Theme.Control}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Accent}"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Style="{StaticResource Theme.ComboToggleButton}"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="6,3,26,3" VerticalAlignment="Center" HorizontalAlignment="Left"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                            <TextBox x:Name="PART_EditableTextBox" Style="{x:Null}" Visibility="Hidden"
                                     Background="Transparent" BorderThickness="0"
                                     Foreground="{TemplateBinding Foreground}" CaretBrush="{DynamicResource Theme.Text}"
                                     Margin="4,3,26,3" VerticalAlignment="Center" Focusable="True"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="Popup" Placement="Bottom" Focusable="False" AllowsTransparency="True"
                                   IsOpen="{TemplateBinding IsDropDownOpen}" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="{DynamicResource Theme.Control}"
                                            BorderBrush="{DynamicResource Theme.Accent}" BorderThickness="1"/>
                                    <ScrollViewer SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="28"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter Property="IsTabStop" Value="False"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource Theme.Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox" BasedOn="{StaticResource {x:Type ListBox}}">
            <Setter Property="IsSynchronizedWithCurrentItem" Value="False"/>
            <Setter Property="Background" Value="{DynamicResource Theme.Control}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Accent}"/>
        </Style>
        <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
        </Style>
        <Style TargetType="TreeView" BasedOn="{StaticResource {x:Type TreeView}}">
            <Setter Property="Background" Value="{DynamicResource Theme.Control}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Accent}"/>
        </Style>
        <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
        </Style>

        <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
        </Style>
        <Style TargetType="RadioButton" BasedOn="{StaticResource {x:Type RadioButton}}">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- ── Buttons: a minimal template so the (leaked-light) content text
             always sits on a themed fill, on light AND dark themes. ──────── -->
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="{DynamicResource Theme.Chrome}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Accent}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"
                                CornerRadius="2" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource Theme.Accent}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource Theme.Accent}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── Tabs: themed headers (selected = Surface, else Chrome). ───── -->
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{DynamicResource Theme.Text}"/>
            <Setter Property="Background" Value="{DynamicResource Theme.Chrome}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="bd" Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource Theme.Accent}"
                                BorderThickness="1,1,1,0" CornerRadius="3,3,0,0"
                                Margin="2,0,0,0" Padding="8,3">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource Theme.Surface}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="False"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource Theme.Accent}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl" BasedOn="{StaticResource {x:Type TabControl}}">
            <Setter Property="Background" Value="{DynamicResource Theme.Window}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Accent}"/>
        </Style>

        <!-- ── Menu / status chrome ─────────────────────────────────────
             The bar plus the dropdown items are themed so the leaked light
             foreground stays readable (a menu item's Background fills its row). -->
        <Style TargetType="Menu" BasedOn="{StaticResource {x:Type Menu}}">
            <Setter Property="Background" Value="{DynamicResource Theme.Chrome}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.ChromeText}"/>
        </Style>
        <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.ChromeText}"/>
            <Style.Triggers>
                <Trigger Property="Role" Value="TopLevelHeader">
                    <Setter Property="Template" Value="{StaticResource Theme.MenuTopHeader}"/>
                </Trigger>
                <Trigger Property="Role" Value="TopLevelItem">
                    <Setter Property="Template" Value="{StaticResource Theme.MenuTopHeader}"/>
                </Trigger>
                <Trigger Property="Role" Value="SubmenuHeader">
                    <Setter Property="Template" Value="{StaticResource Theme.MenuSubHeader}"/>
                </Trigger>
                <Trigger Property="Role" Value="SubmenuItem">
                    <Setter Property="Template" Value="{StaticResource Theme.MenuSubItem}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ContextMenu" BasedOn="{StaticResource {x:Type ContextMenu}}">
            <Setter Property="Background" Value="{DynamicResource Theme.Chrome}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.ChromeText}"/>
        </Style>
        <Style TargetType="StatusBar" BasedOn="{StaticResource {x:Type StatusBar}}">
            <Setter Property="Background" Value="{DynamicResource Theme.Chrome}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.ChromeText}"/>
        </Style>
        <Style TargetType="ToolBar" BasedOn="{StaticResource {x:Type ToolBar}}">
            <Setter Property="Background" Value="{DynamicResource Theme.Chrome}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.ChromeText}"/>
        </Style>
        <Style TargetType="ToolBarTray" BasedOn="{StaticResource {x:Type ToolBarTray}}">
            <Setter Property="Background" Value="{DynamicResource Theme.Chrome}"/>
        </Style>

        <Style TargetType="GroupBox" BasedOn="{StaticResource {x:Type GroupBox}}">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Background" Value="{DynamicResource Theme.Surface}"/>
            <Setter Property="Foreground" Value="{DynamicResource Theme.AccentText}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Theme.Accent}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!--
            Historical note (kept from the original light-only theme): every
            left-hand record list and each shared option combo binds to a
            collection's default ICollectionView, so leaving
            IsSynchronizedWithCurrentItem at its WPF default made selectors on
            different tabs fight over the shared CurrentItem (adding a Map
            Button once yanked the editor to the Places tab). The ListBox /
            ComboBox styles above set it False, severing that coupling with no
            behavioural loss since every selector uses an explicit
            SelectedItem / SelectedValue binding.
        -->
    </Application.Resources>
</Application>
